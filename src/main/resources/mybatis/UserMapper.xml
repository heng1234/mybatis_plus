<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hlvy.mybatis_plus.mapper.UserMapper" >
    <select id="selectAll" resultType="com.hlvy.mybatis_plus.entity.User">
           select * from user ${ew.customSqlSegment}
    </select>
    <select id="selectUserPage" resultType="com.hlvy.mybatis_plus.entity.User">
            select * from user ${ew.customSqlSegment}
    </select>
</mapper>